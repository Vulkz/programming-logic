algoritmo "48_exercise"

var
cont, num, soma:inteiro
inicio
cont <- 1
soma <- 0

repita
escreva("Digite o", cont, " numero: ")
leia(num)
soma <- soma + num
cont <- cont + 1
ate cont = 7 + 1

escreva("Total igula a", soma)
fimalgoritmo
